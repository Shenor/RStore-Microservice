const userRoutes = require('./user.controller');
const configRoutes = require('./config.controller');
const integrationRoutes = require('./integration.controller');

module.exports = {
  userRoutes,
  configRoutes,
  integrationRoutes
}
